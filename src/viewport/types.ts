export type Coords = { x: number; y: number };
export type Bounds = { width: number; height: number };
export type Limits = { minx: number; miny: number; maxx: number; maxy: number };
export type Listener = { removeListeners: () => void };
